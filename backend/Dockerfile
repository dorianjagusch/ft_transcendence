FROM alpine:latest

#Install venv for python: sudo apt install python3.10-venv
#Create a virtual environment: python3 -m venv .venv
#Activate virtual environment: . .venv/bin/activate
#Install Django: pip install django
#Install Django rest framework: pip install djangorestframework
#Create a django project: django-admin startproject backend .
#Go to file backend: cd backend/
#Open a new console ->
#Migrate db: python manage.py migrate
#Create an admin user for the project: python manage.py createsuperuser
#Migrate models into the db: python manage.py makemigrations backend
#Migrate new models to the db: python manage.py migrate

#To start the server, activate venv and run server: . .venv/bin/activate && python manage.py runserver
